# <%- pkgName %>

OpenAPI v3 规范文档（JSON 格式），作为 `<%- pkgScope %>` 系列请求包的接口定义基础。

## 特性

- 完整的 OpenAPI v3 规范 JSON 文档
- 提供 TypeScript 类型定义（基于 [openapi-types](https://www.npmjs.com/package/openapi-types)）
- 作为 `<%- axiosPkgName %>` 和 `<%- unPkgName %>` 的接口定义来源

## 安装

```bash
npm install -D <%- pkgName %>
# 或者
yarn add -D <%- pkgName %>
# 或者
pnpm add -D <%- pkgName %>
```

### peerDependencies

本包需要自行安装以下开发依赖：

```bash
npm install -D openapi-types@^12.1.3
# 或者
yarn add -D openapi-types@^12.1.3
# 或者
pnpm add -D openapi-types@^12.1.3
```

## 使用示例

```ts
import type { OpenAPIV3 } from 'openapi-types'
import document from '<%- pkgName %>'

// document 即为完整的 OpenAPI v3 规范对象
const paths = document.paths
const schemas = document.components?.schemas
```

## 关联包

| 包名 | 说明 |
|------|------|
| [<%- axiosPkgName %>](<%- axiosUrl %>) | 基于 Axios 的网络请求模块 |
| [<%- unPkgName %>](<%- unUrl %>) | uni-app 网络请求模块 |

## License

MIT
